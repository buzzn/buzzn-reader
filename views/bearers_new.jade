extends layout

block content
    h1= title
    form#formAddUser(name="create Bearer",method="post",action="/bearers/create")
        input#inputUserName(type="text", placeholder="token", name="token")
        button#btnSubmit(type="submit") submit
